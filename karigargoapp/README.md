# KarigarGo (web)

Vite + React + TypeScript + Tailwind + Supabase.

## Setup

```bash
npm install
cp .env.example .env.local
```

Edit `.env.local` with your Supabase project URL and anon key (from the Supabase dashboard). Do not commit `.env.local`.

## Scripts

```bash
npm run dev    # development server
npm run build  # production build
```

## Expo reference UI

An older Expo-based UI prototype lives in `../karigargoapp-old-ui/` for design reference only.
